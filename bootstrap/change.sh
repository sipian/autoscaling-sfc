#!/bin/bash

chmod +x bootstrap
chmod +x bootstrap-common-functions
chmod +x bootstrap-deb-functions
chmod +x bootstrap-src-functions
./bootstrap develop --config-file=/home/ubuntu/bootstrap-config-file